<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ludovic.psvsxbox">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PSVsXBOX">
        <activity
            android:name=".XBOX"
            android:label="@string/title_activity_x_b_o_x"
            android:theme="@style/Theme.PSVsXBOX.NoActionBar"></activity>
        <activity
            android:name=".PS"
            android:label="@string/title_activity_p_s"
            android:theme="@style/Theme.PSVsXBOX.NoActionBar" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>